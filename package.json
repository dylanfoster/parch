{
  "name": "parch",
  "version": "0.0.1",
  "description": "Restify + Sequelize",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "test": "mocha --recursive",
    "watch": "chokidar 'lib/**' 'test/**' -c 'npm test' --initial"
  },
  "keywords": [
    "restify",
    "sequelize",
    "restful"
  ],
  "author": "Dylan Foster",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "chai": "^3.5.0",
    "chokidar-cli": "^1.2.0",
    "eslint": "^2.12.0",
    "eslint-config-unstandard": "^1.3.2",
    "jsdoc": "^3.4.0",
    "mocha": "^2.5.3"
  },
  "eslintConfig": {
    "extends": "unstandard"
  }
}
